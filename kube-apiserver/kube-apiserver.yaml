name: kube-apiserver
version: 'KUBE_VERSION'
summary: kube-apiserver
description: kube-apiserver
grade: devel
confinement: strict

apps:
  kube-apiserver:
    command: run-kube-apiserver
    daemon: simple
    plugs:
        - home
        - network
        - network-bind
parts:
  kube-apiserver:
    plugin: dump
    source: build
